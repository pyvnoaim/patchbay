{
  "name": "patchbay",
  "version": "0.0.1",
  "description": "Every host, one jack away.",
  "type": "module",
  "bin": {
    "bay": "src/cli.ts"
  },
  "engines": {
    "node": ">=22.6"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "dev": "node --env-file=dev/dev.env scripts/tauri.mjs dev",
    "build": "tauri build",
    "icon": "node scripts/icon.mjs && tauri icon assets/icon.png",
    "icons": "node scripts/icons.mjs",
    "vendor": "node scripts/vendor.mjs",
    "brands": "node scripts/brands.mjs",
    "cli": "node --env-file=dev/dev.env --experimental-strip-types --disable-warning=ExperimentalWarning src/cli.ts",
    "test": "npm run test:cli && npm run test:app",
    "test:cli": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test",
    "test:app": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "dependencies": {
    "smol-toml": "^1.3.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "lucide-static": "^1.37.0",
    "simple-icons": "^16.29.0"
  }
}
